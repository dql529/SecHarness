# E3-noRF-14B: Qwen2.5-14B + Harness Loop (NO RF tool)
# Tests HE framework value WITHOUT the dominant ML classifier
# Isolates contribution of: signature matching, knowledge base, history, permissions, structured loop
experiment:
  id: "E3_noRF_14B_unsw"
  name: "Zero-shot 14B + Harness, No RF Tool"

model:
  base: "api://localhost:11434/qwen2.5:14b"
  adapter: null
  load_in_4bit: false
  max_new_tokens: 512
  temperature: 0.1

harness:
  enabled: true
  rf_context: false
  tools:
    check_anomaly: false
    lookup_signature: true
    query_history: true
    load_knowledge: true
  permissions:
    enabled: true
    confidence_threshold: 0.3
  max_steps: 5

data:
  dataset: "unsw"
  split: "val"
  subsample: null
  seed: 42
  ml_model_path: "project/logs/E2_beta_ml_model.pkl"

output:
  log_dir: "project/logs/v2/"
  results_dir: "project/results/tables/v2/"
