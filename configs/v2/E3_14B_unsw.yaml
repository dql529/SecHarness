# E3-14B: Qwen2.5-14B (Ollama) + SecHarness
# Harness value at 14B scale — same family as 7B/32B for controlled comparison
experiment:
  id: "E3_14B_unsw"
  name: "Zero-shot 14B Qwen, SecHarness"

model:
  base: "api://localhost:11434/qwen2.5:14b"
  adapter: null
  load_in_4bit: false
  max_new_tokens: 256
  temperature: 0.1

harness:
  enabled: true
  rf_context: false
  tools:
    check_anomaly: true
    lookup_signature: true
    query_history: true
    load_knowledge: true
  permissions:
    enabled: true
    confidence_threshold: 0.3
  max_steps: 5

data:
  dataset: "unsw"
  split: "val"
  subsample: null
  seed: 42
  ml_model_path: "project/logs/E2_beta_ml_model.pkl"

output:
  log_dir: "project/logs/v2/"
  results_dir: "project/results/tables/v2/"
